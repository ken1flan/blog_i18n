en:
  activemodel:
    models:
      forms/session: Session
    attributes:
      forms/session:
        login_id: Login id
        password: password
    errors:
      models:
        forms/session:
          attributes:
            base:
              login_id_or_password_is_wrong: Login ID or password is incorrect.
